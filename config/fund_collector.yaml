# 基金数据采集器配置
# 数据库连接: config/.env → DATABASE_URL
fund_collector:

  target_fund_types:
    - "股票型"
    - "混合型"
    - "指数型"

  lookback_years: 2
  request_delay_seconds: 0.5
  progress_every: 20
  max_retries: 3
