# =============================================================================
# Docker Compose 环境变量 — 复制本文件为 .env 后填写真实值
#   cp docker/.env.example docker/.env
#
# docker/.env 已加入 .gitignore，请勿提交到 Git
# =============================================================================

# PostgreSQL 容器密码
POSTGRES_PASSWORD=changeme
